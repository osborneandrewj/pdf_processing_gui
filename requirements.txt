# requirements.txt
Flask==2.3.3
PyPDF2==3.0.1
# or whatever PDF library you're using for form_utils

# runtime.txt (for Heroku)
python-3.11.0

# Procfile (for Heroku)
web: python app.py

# Dockerfile (for Railway/Render)
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

EXPOSE 5000

CMD ["python", "app.py"]

# railway.toml (for Railway)
[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"

# .gitignore
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv
pip-log.txt
pip-delete-this-directory.txt
.tox
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.log
.git
.mypy_cache
.pytest_cache
.hypothesis

# Instance folder
instance/

# Environment variables
.env
.flaskenv

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project specific
test_pdfs/
output/
*.pdf
*.txt
